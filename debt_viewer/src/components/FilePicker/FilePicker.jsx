// Full implementation in Step 9.
// Accepts onDocumentLoad(normalizedDocument, warnings) and onError(message).
function FilePicker({ onDocumentLoad, onError }) {
  return null;
}

export default FilePicker;
